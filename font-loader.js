(() => {
  // Brand font family names are declared in fonts.css.
  // Binary font payloads are intentionally not shipped in the public repository.
  document.documentElement.classList.add('brand-fonts-local');
})();
